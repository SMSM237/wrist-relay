# Security

## 경계

Wrist Relay는 알림 내용을 기기 밖으로 전송하지 않습니다. 알림 접근 서비스, 로컬 Room DB, Android Keystore, 시스템 알림 게시 기능만 사용합니다.

## 보호 조치

- `INTERNET`, 위치, 연락처, SMS, 통화기록, Bluetooth, NFC 권한 없음
- `BIND_NOTIFICATION_LISTENER_SERVICE`로 시스템만 알림 리스너 서비스에 바인딩
- AES-256-GCM, 행 ID와 필드 유형을 AAD로 결합해 필드 바꿔치기 방지
- 알림 키는 원문 대신 SHA-256 해시 저장
- `allowBackup=false`, 데이터 추출·전체 백업 제외, cleartext 차단
- R8 축소 릴리스와 릴리스 전용 `FLAG_SECURE`
- 잠금화면 공개 버전에는 일반 문구만 표시
- 진단 내보내기에서 콘텐츠와 앱 식별자를 제외

## 남은 플랫폼 경계

휴대폰 앱만으로 Galaxy Watch의 연결 상태나 실제 햅틱 성공을 신뢰성 있게 읽을 수 없습니다. 따라서 앱은 테스트 알림을 전송한 뒤 사용자의 명시적 확인을 요구합니다. 휴대폰 알림을 조용히 하는 동작은 Galaxy Wearable의 `휴대전화 알림 무음` 설정에 의존합니다.

## 취약점 보고

민감한 알림 내용, DB 파일, 키 자료를 공개 이슈에 첨부하지 마세요. 재현 절차는 앱의 `개인정보 제외 진단 공유` 결과와 함께 비공개 채널로 전달해야 합니다.
